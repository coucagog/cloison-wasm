[package]
name = "cloison-wasm"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/coucagog/cloison-wasm"
description = "CLOISON — wrapper wasm-bindgen de cloison-core (edge navigateur / embarqué)."

[dependencies]
